spring:
  application:
    name: wepro
  datasource:
    url: jdbc:mysql://localhost:3306/wepro?createDatabaseIfNotExist=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

  security:
    debug: true
    oauth2:
      client:
        registration:
          kakao:
            client-id: 3c33a9a67c89051d03046c402d256eae
            client-secret: 4g96Wg97wEvRlPq4LESlvAxiIOEFX84V
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: http://127.0.0.1:8080/login/oauth2/code/kakao
            scope: openid
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v1/oidc/userinfo
            user-name-attribute: sub
            jwk-set-uri: https://kauth.kakao.com/.well-known/jwks.json



logging:
  level:
    org.springframework.web.client.RestTemplate: DEBUG